<view>
    <van-tabbar active="{{ active }}" bind:change="onChange">
        <van-tabbar-item class="tab-item" wx:for="{{list}}" wx:key="index" icon="{{item.icon}}" data-path="{{item.pagePath}}">
            {{item.text}}
        </van-tabbar-item>
    </van-tabbar>
</view>